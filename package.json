{
  "name": "playwright-mcp-server",
  "version": "1.0.0",
  "scripts": {
    "start": "npx @playwright/mcp@latest --port $PORT --host 0.0.0.0 -- --no-sandbox --disable-setuid-sandbox --disable-dev-shm-usage --disable-gpu --single-process --no-zygote"
  },
  "dependencies": {
    "@playwright/mcp": "latest"
  }
}
