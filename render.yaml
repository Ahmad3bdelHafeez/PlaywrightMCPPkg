services:
  - type: web
    name: playwright-mcp
    runtime: node
    plan: starter
    buildCommand: npm install && npx playwright install chromium --with-deps
    startCommand: npm start
    envVars:
      - key: PORT
        value: 8931
